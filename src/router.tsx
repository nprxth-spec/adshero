import { Link as RouterLink, useNavigate as useRouterNavigate, useLocation as useRouterLocation } from 'react-router-dom';

// Simple re-exports to support custom imports
export const Link = RouterLink;
export const useNavigate = useRouterNavigate;
export const useLocation = useRouterLocation;
